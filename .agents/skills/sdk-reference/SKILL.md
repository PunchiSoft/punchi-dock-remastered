---
name: sdk-reference
description: Alias de compatibilidad para solicitudes que mencionen literalmente la skill `sdk-reference`. Para investigar APIs y patrones en el SDK local de KDE usar `kde-sdk-reference`; no activar esta skill genérica en tareas nuevas.
---

# Alias de referencia SDK

Usar `kde-sdk-reference` como skill principal. Consultar documentación externa solo si la copia local no permite resolver la cuestión o se necesita verificar una versión.
